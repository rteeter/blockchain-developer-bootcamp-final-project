// portfolio tracker app, aggregating wallets

address[] public arr;
// list of tokens per address
// wrapped tokens variables (how many accepted types?)


function getAddresses(address, address, address) {
    // validate addresses
    // register addresses
    // if validated, store in arr
}

function getBalance(arr) {
    // loop through each address in the array
        // getting the token balances from each address
        // add token balances from each address into temporary lists of tokens per address
}

function wrapTokens(lists) {
    // loop through lists placing matching tokens in one variable
        // example: ETH from each address into one wrapped token variable
    // store wrapped variables in local storage
}


// UI form to take in addresses
// UI display wrapper tokens in a dashboard
// UI set targets for goal wrapper tokens
// UI display wrapper to goal wrapper 
